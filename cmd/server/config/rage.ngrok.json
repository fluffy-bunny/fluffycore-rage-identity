{
  "oidcConfig": {
    "baseUrl": "https://${DOMAIN}"
  },
  "selfIDPConfig": {
    "authority": "https://${DOMAIN}",
    "redirectUrl": "${NGROK_URL}/auth/callback"
  },
  "systemConfig": {
    "domain": "${DOMAIN}",
    "developerMode": false
  },
  "webAuthNConfig": {
    "rpDisplayName": "RAGE",
    "rpID": "${DOMAIN}",
    "rpOrigins": ["https://${DOMAIN}"]
  }
}
