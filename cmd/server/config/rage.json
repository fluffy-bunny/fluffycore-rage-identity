{
  "corsConfig": {
    "allowedOrigins": [
      "https://${DOMAIN}",
      "http://localhost:3000",
      "http://localhost:3001"
    ]
  },
  "emailVerificationRequired": true,
  "multiFactorRequired": true,
  "multiFactorRequiredByEmailCode": true,
  "systemConfig": {
    "domain": "localhost",
    "developerMode": false
  },
  "totp": {
    "enabled": false,
    "issuerName": "EXAMPLE.RAGE.IDENTITY"
  },
  "webAuthNConfig": {
    "rpDisplayName": "RAGE",
    "rpID": "${DOMAIN}",
    "rpOrigins": ["https://${DOMAIN}"]
  }
}
